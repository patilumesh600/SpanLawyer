// import { selectProductResultsDomain } from '../selectors';

describe('selectProductResultsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
