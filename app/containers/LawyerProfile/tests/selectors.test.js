// import { selectLawyerProfileDomain } from '../selectors';

describe('selectLawyerProfileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
